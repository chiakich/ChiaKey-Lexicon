name: Add bigram

on:
  issues:
    types:
      - opened
      - edited
      - reopened

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  propose:
    name: Check report and propose PR
    runs-on: ubuntu-latest
    if: contains(github.event.issue.labels.*.name, 'Add bigram') || startsWith(github.event.issue.title, '長句選字錯誤')

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Install release dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libsqlite3-dev opencc

      - name: Build release DB
        run: cargo run --release -- prepare-release

      - name: Process bigram report
        id: report
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_USER: ${{ github.event.issue.user.login }}
          BIGRAM_REPORT_OUT_DIR: ${{ runner.temp }}/bigram-report
        run: node scripts/lexicon/process-bigram-issue.mjs

      - name: Comment on issue
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ -f "${{ runner.temp }}/bigram-report/comment.md" ]; then
            gh issue comment "${{ github.event.issue.number }}" --body-file "${{ runner.temp }}/bigram-report/comment.md"
          fi
        if: always()

      - name: Create pull request
        if: steps.report.outputs.should_pr == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ github.token }}
          branch: bot/bigram-report-${{ github.event.issue.number }}
          delete-branch: true
          title: ${{ steps.report.outputs.pr_title }}
          body-path: ${{ runner.temp }}/bigram-report/pr-body.md
          commit-message: ${{ steps.report.outputs.commit_message }}
          add-paths: sources/chiaki-modern-overlay/bigrams.tsv
          labels: |
            Add bigram
