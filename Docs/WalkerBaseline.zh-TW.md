# Walker 準確率基準線

改動詞庫之前先跑這份，改完再跑一次比對。單一數字沒有意義，**要看的是同一份 gold set 在兩次之間的差**。

工具是 `ChiaKey` repo 的 `WalkerGoldSet --word-level`，方法與取捨寫在該檔案的區塊註解。

## 建立方式

```bash
# 在 ChiaKey repo
T=ChiaKey-Source/Frameworks/Manjusri/Tools
Scripts/eval-walker-goldset.sh \
  --lexicon ../ChiaKey-Lexicon/dist/dev/ChiaKeySource-dev.db \
  --corpus  ../ChiaKey-Lexicon/sources/chiaki-tw-homophone-bigram/pipeline/data/<語域>.txt \
  --out     ../ChiaKey-Lexicon/tmp/goldset/<語域>.tsv \
  --word-level --pins "$T/goldset-pins.tsv" --variants "$T/goldset-variants.tsv" \
  --min-chars 4 --max-chars 40 --limit 50000 \
  --mismatches ../ChiaKey-Lexicon/tmp/goldset/<語域>-miss.tsv
```

gold set 含語料原文，**不進版控**（`tmp/` 已 gitignore）。要重現得先依 `sources/chiaki-tw-homophone-bigram/pipeline/README.md` 重建語料。

## 基準線（2026-08-02）

詞庫：`dist/dev/ChiaKeySource-dev.db`，即 `fix/rime-variant-policy` 分支重建後的狀態。

| 語域 | 來源 | gold 列 | 逐句正確 | 逐字正確 |
| --- | --- | --- | --- | --- |
| written | 新北市新聞（test 半） | 2,684 | 63.38% | 95.25% |
| spoken | 立法院公報詢答 | 67,018 | 73.22% | 96.92% |
| forum | PTT | 62,608 | 69.39% | 96.35% |
| wiki | 中文維基 | 53,029 | 68.64% | 94.98% |
| casual | Plurk | 13,531 | 72.82% | 95.78% |

合計 198,870 列。

## 錯誤結構

58,399 個錯誤句、89,802 次字級替換、8,158 種混淆對。前 25 種只佔 24.6%，是長尾。

**幾乎沒有斷詞層級的錯誤**：輸出與期望長度不同的只有 1 句（0.0%）。walker 的問題是**同讀音候選選錯**，不是切錯詞。這決定了改善的著力點在候選排序與上下文，不在斷詞。

最大的幾群：

| 混淆 | 次數 | 性質 |
| --- | --- | --- |
| 啊→阿、啦→拉、嘛→嗎 | 7,204 | 句末助詞輸給同音實詞。位置訊息強，是上下文層最該修的一群 |
| 臺→台 | 2,170 | **不是錯誤**，見下 |
| 再↔在 | 2,723 | 雙向都有，典型需要上下文 |
| 的→得、得→的、地→的 | 2,367 | 的/地/得。已知待輸入法本體以聲調區分，bigram 層刻意不處理 |
| 她→他、它→他、他→它 | 2,504 | 人稱/指代。上下文也未必判得出，不宜由 unigram 決定 |

## 讀這份數字時的五個前提

1. **`臺→台` 那 2,170 次不是 walker 錯誤，是政策生效。** gold set 的期望文字取自語料原文，維基與公報原文寫「臺」，而本專案政策選「台」。集中在 wiki（1,067／16,630 錯誤）與 spoken（1,035／17,945），其餘語域極少。
   附帶暴露一個真實問題：整詞在詞庫裡的機構名（`臺灣師範大學`）保住「臺」，但組合出來的（`國立臺北大學法律學院`）會被拆成 `臺北`→`台北`。專有名詞未完全豁免。

2. **bigram 層是用這些語料訓練的**，所以這份基準線對 `chiaki-tw-homophone-bigram` 而言是 in-sample，絕對值偏高。用於比較改動前後仍然有效（偏差在兩側相同），但不可當作對外的準確率。

3. **written 只有 2,684 列**，比其他語域小一到兩個數量級。新聞句子長，落在 4–40 字範圍的本來就少。該語域的差異要大於約 ±1% 才有意義。

4. **gold set 本身有取材偏差**。含上下文才能定讀音的多音詞（`數`、`更`、`曲`、`得`）整句排除，共 18,185 個詞條落在 reject 桶。這是明說的取捨，不是疏漏。

5. **讀音來自詞庫本身**，不是 moedict。這是刻意的：教育部標準讀音與台灣實際讀音有系統性差異（`tmp/moedict-mismatches.tsv` 記錄 8,820 筆），拿標準讀音當標籤會在 walker 其實答對的位置判它錯。代價是標註與受測對象共用同一份讀音資料。

## 比對紀錄

### 2026-08-02　bigram 抽取的斷詞詞長加成修正

`pipeline/src/main.rs` 的加成由每字 +1.0 改為每多出一字 +1.0，對齊 `Node::lengthPrior()`，重新抽取並重建資料列（292,639 → 339,383）。**沿用同一批 gold set**，只換詞庫。

| 語域 | 逐句正確 | Δ | 逐字正確 | Δ |
| --- | --- | --- | --- | --- |
| written | 63.38% → 64.12% | +0.75 | 95.25% → 95.35% | +0.10 |
| spoken | 73.22% → **75.29%** | **+2.07** | 96.92% → 97.12% | +0.21 |
| forum | 69.39% → **70.27%** | **+0.88** | 96.35% → 96.42% | +0.08 |
| wiki | 68.64% → 68.58% | −0.06 | 94.98% → 94.96% | −0.01 |
| casual | 72.82% → **73.57%** | **+0.75** | 95.78% → 95.88% | +0.10 |

四升一平。三個大樣本語域全部改善，維基持平。written 的 +0.75 落在該語域的雜訊範圍內，不算數。

抽取端的變化：token 數 −8.7%（斷詞開始偏好長詞）、相異詞對 +30%、複合詞對 −93%（斷詞自己把複合詞併成單一 token，步驟 4 幾乎無事可做）。

### 2026-08-02　權重相同的候選改由國教院詞頻決定順序

`db::reorder_unigrams`，見 `sources/naer-word-frequency/README.md`。權重完全不動。

| 語域 | 逐句正確 | Δ |
| --- | --- | --- |
| written | 64.31% → 64.57% | +0.26 |
| spoken | 74.83% → **75.25%** | +0.41 |
| forum | 69.62% → **70.41%** | +0.80 |
| wiki | 67.43% → **68.79%** | **+1.36** |
| casual | 72.98% → **73.62%** | +0.64 |

五升零降。

### 2026-08-02　嘗試填入 Katz backoff：無效，已放棄

引擎的 `Node::findHighestScorePair` 會把 unigram 路徑分數加上 `BOW(previous)`，而 DB 的 `backoff` 欄一直是 0。填入語料算出的 Katz 權重（`beta / (1 - 已見接續的機率質量)`）**對輸出沒有任何影響**：spoken 在 backoff 全 0、全 −0.2、逐上下文 Katz（均值約 −0.5）三種設定下逐句正確數完全相同（50,836）。到 −2.0 才有反應，而那是災難（54.51%）。

分數落差通常大於 0.5，小於這個量級的位移改變不了任何 argmax；大到能改變的量級會直接摧毀路徑搜尋。中間沒有可用區間。實作已撤除。

**這次嘗試的副產品是上面那條 tie 修正**，以及〈量測方法的陷阱〉那一節。

### 2026-08-02　重建 gold set，記錄補讀音之後的基準線

`efc657b` 補了 33 列單字讀音、壓下 11 列上游讀音錯誤、刪了 3 列自有層錯誤資料之後重建。**gold set 重新產生**（先前那批產生於 02:01，落後 `379c25f` 與 `f57bfc6` 兩次改動）。

| 語域 | gold 列 | 逐句正確 | 逐字正確 |
| --- | --- | --- | --- |
| written | 2,684 | 64.5678% | 95.3798% |
| spoken | 67,018 | 75.2484% | 97.1192% |
| forum | 62,608 | 70.4127% | 96.4403% |
| wiki | 53,029 | 68.7850% | 94.9951% |
| casual | 13,531 | 73.6161% | 95.8842% |

五個語域的 gold 列數與最初的基準線完全相同，逐句正確率也全部落在前一筆記錄的四捨五入誤差內。**補讀音對句級準確率沒有可測量的影響。**

這不是意外，而是測量方法的必然：見下方〈gold set 測不到什麼〉。

## gold set 測不到什麼

**補單字讀音的價值，這份 gold set 在結構上就測不到。**

前提 4 會把含多音字的句子整句排除。補一個讀音正好讓該字從單音變多音，於是含這個字的句子從此不再受測——新增的讀音必然落在測試集之外。`efc657b` 讓 18 個原本單音的字（`塢`、`脂`、`髓`、`獷`、`砧`、`絢`、`骼` 等）變成多音，正是這個情形。

這些改動的價值在別的地方：該字能不能單獨以該讀音輸入、該詞能不能打得出來。要驗收得換方法（例如逐條檢查目標讀音下候選清單是否含該字），不能指望這份句級準確率。

上游讀音錯誤的降權同理：壓下去的是本來就不該出現的候選，正確率不會動。

**附帶的風險**：補讀音會讓合格句減少，理論上會縮小 gold set，使兩次比對不是同一份測試集。這次沒有發生（列數完全相同——那批字太罕見，不出現在 4–40 字全漢句的合格池裡），但改動涉及常用字時必須先確認列數，列數變了就不能直接跟舊表比。

### 2026-08-02　`啊` 在ㄚ上提升到 `阿` 之上

`explicit.tsv` 加一列 `91 啊 -1.00`（原 `啊 -1.673216` 輸給 `阿 -1.014633`）。候選由 `scripts/audit/audit-single-char-heads.mjs` 找出，方向由國教院詞頻判定（`啊` 1172.9 vs `阿` 9.6）。**沿用同一批 gold set**，只換詞庫。

| 語域 | 逐句正確 | Δ | 逐字正確 | Δ |
| --- | --- | --- | --- | --- |
| written | 64.5678% → 64.5678% | 0.00 | 95.3798% → 95.3798% | 0.00 |
| spoken | 75.2484% → **77.6224%** | **+2.37** | 97.1192% → 97.3553% | +0.24 |
| forum | 70.4127% → **71.7528%** | **+1.34** | 96.4403% → 96.5750% | +0.13 |
| wiki | 68.7850% → 68.7963% | +0.01 | 94.9951% → 94.9947% | −0.00 |
| casual | 73.6161% → **74.3182%** | **+0.70** | 95.8842% → 95.9735% | +0.09 |

四升一平。字級替換總數 87,153 → 84,067（−3,086）：

```
啊→阿   3,100 → 3     −3,097
阿→啊       0 → 12       +12
```

**翻轉的代價只有 12 次。** 事前依 gold set 出現次數估的上限是 659，實際低 55 倍——`阿` 幾乎總是出現在 `阿姨`、`阿公`、`阿拉伯` 這類詞裡，那些以整詞路徑勝出，不受單字排頭影響。副作用乾淨，其餘混淆對最大變動是 `廷→亭` +1。

written 一個字都沒變：新北市新聞的 test 半不出現句末語氣詞。

**方法論上的教訓**：翻轉單字排頭是雙向的，現在的排頭永遠不會錯，翻過去之後換它每次都錯。所以判準不是「翻轉成本多低」而是「哪一邊才是使用者要的」。同一份稽核清單裡翻轉成本最低的兩個（`嘛`／`嗎` 差 0.000008、`呦`／`優` 差 0.000026）淨值都是負的——`嗎` 在語料裡比 `嘛` 常用四倍以上，翻過去要賠上八千次。工具的排序已改為依國教院詞頻的方向，翻轉成本只當次要鍵。

## 量測方法的陷阱

**不要用 `sqlite3 UPDATE` 改資料庫來做 A/B。** UPDATE 會搬動資料列的實體位置，而權重相同的候選由實體順序決定勝負，因此會產生約 ±1 個百分點的假訊號——大於本專案多數改動的真實效果。

實例：完全沒有 `backoffs.tsv` 的 build 是 74.83%，全填 0（有效值相同）的 build 是 75.85%，差 1.02 點，純粹因為多跑了 269,774 次 UPDATE。

比對必須兩邊都是完整 `prepare-release`，且兩邊的 UPDATE 行為相同。`prepare-release` 本身是決定性的（相同輸入產生相同的資料列順序），已驗證。

國教院排序修正之後，tie 不再隨實體順序浮動，這個陷阱的影響應會大幅下降，但方法論上的要求不變。

## 已知會影響下次比對的待辦

- unigram 權重不是機率分布，Katz backoff 無法直接套用
